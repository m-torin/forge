/**
 * Next.js server exports
 */

// For now, Next.js server analytics uses the same implementation as regular server
// In the future, we can add Next.js-specific server features here

// Re-export everything from server
export * from './server';

// Export types
export type * from './types';
