// Export Mantine Button directly
export { Button, type ButtonProps } from '@mantine/core';

// Keep buttonVariants export for backward compatibility
// This can be removed once all usages are migrated
export const buttonVariants = () => '';
