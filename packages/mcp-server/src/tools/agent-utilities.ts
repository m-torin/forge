/**
 * MCP Utils - Agent Utility Tools
 *
 * DEPRECATED: Agent utility MCP tools removed.
 *
 * Rationale: These were unnecessary wrappers around TypeScript utilities.
 * Code should import utilities directly from '../utils/agent-helpers' instead.
 *
 * Removed tools:
 * - extractObservationTool (obsolete, inline parsing is clearer)
 * - createEntityNameTool (import createEntityName directly)
 * - validateAgentRequestTool (import validateAgentRequest directly)
 * - formatAgentResponseTool (import formatAgentResponse directly)
 */

// This file intentionally left empty - to be removed in future cleanup
