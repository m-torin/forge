{
  "extends": "../../packages/typescript-config/library.json", // Use relative path
  "include": [
    "**/*.ts",
    "ambient.d.ts" // Include local ambient declarations
  ],
  "exclude": ["node_modules"]
}
