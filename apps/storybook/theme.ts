import { createTheme } from '@mantine/core';

export const theme = createTheme({
  defaultRadius: 'md',
  // Customize theme as needed to match the existing design system
  // This is a basic theme that can be extended later
  fontFamily: 'var(--font-sans)',
  primaryColor: 'blue',
});
