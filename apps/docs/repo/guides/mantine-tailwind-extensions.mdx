---
title: Mantine v8 Extensions Guide
description:
  Comprehensive guide for Mantine v8 extensions that provide enhanced
  functionality beyond core components
---

# Mantine v8 Extensions Guide

## Overview

This guide covers the Mantine v8 extensions that provide enhanced functionality
beyond the core components. These extensions include providers for global state
management and specialized components for rich content editing.

## 🎯 **Extensions Included**

### 1. **ModalProvider** - Global Modal Management

### 2. **NotificationsProvider** - Toast Notifications System

### 3. **Carousel** - Enhanced Image/Content Slider

### 4. **RichTextEditor** - TipTap-based Rich Text Editor

---

## 📋 **ModalProvider**

### **Purpose**

Provides global modal management with consistent styling and behavior across
your application.

### **Features**

- ✅ **Global Modal State** - Manage modals from anywhere in your app
- ✅ **Consistent Styling** - Tailwind-like design with smooth animations
- ✅ **Accessibility** - Proper focus management and keyboard navigation
- ✅ **Backdrop Blur** - Modern backdrop with blur effect
- ✅ **Responsive Design** - Adapts to different screen sizes

### **Usage**

```typescript
import { ModalProvider, useModals } from '@mantine/core';

// Wrap your app
function App() {
  return (
    <ModalProvider>
      <YourAppContent />
    </ModalProvider>
  );
}

// Use in components
function MyComponent() {
  const modals = useModals();

  const openModal = () => {
    modals.openModal({
      title: 'Example Modal',
      children: (
        <div>
          <p>This is a modal opened via ModalProvider</p>
          <Button onClick={() => modals.closeModal()}>Close</Button>
        </div>
      ),
    });
  };

  return <Button onClick={openModal}>Open Modal</Button>;
}
```

### **Configuration Options**

```typescript
<ModalProvider
  radius="md"           // Border radius
  shadow="lg"           // Shadow depth
  padding="md"          // Content padding
  centered={true}       // Center modal
  closeOnClickOutside={true}  // Close on backdrop click
  closeOnEscape={true}        // Close on Escape key
  withCloseButton={true}      // Show close button
/>
```

---

## 🔔 **NotificationsProvider**

### **Purpose**

Provides a toast notification system for displaying temporary messages to users.

### **Features**

- ✅ **Multiple Positions** - Top/bottom, left/right/center positioning
- ✅ **Auto-dismiss** - Configurable auto-close timing
- ✅ **Smooth Animations** - Slide-in/out animations
- ✅ **Icon Support** - Custom icons for different notification types
- ✅ **Stacking** - Multiple notifications stack properly

### **Usage**

```typescript
import { NotificationsProvider, useNotifications } from '@mantine/core';

// Wrap your app
function App() {
  return (
    <NotificationsProvider>
      <YourAppContent />
    </NotificationsProvider>
  );
}

// Use in components
function MyComponent() {
  const notifications = useNotifications();

  const showNotification = () => {
    notifications.show({
      title: 'Success!',
      message: 'Your action was completed successfully',
      color: 'green',
      icon: <CheckIcon size={16} />,
    });
  };

  return <Button onClick={showNotification}>Show Notification</Button>;
}
```

### **Configuration Options**

```typescript
<NotificationsProvider
  position="top-right"    // Position: top-left, top-right, top-center, bottom-left, bottom-right, bottom-center
  zIndex={1000}          // Z-index for notifications
  containerWidth={320}   // Max width of notification container
  autoClose={4000}       // Auto-close delay in milliseconds
/>
```

### **Notification Types**

```typescript
// Success notification
notifications.show({
  title: 'Success',
  message: 'Operation completed',
  color: 'green',
  icon: <CheckIcon size={16} />,
});

// Error notification
notifications.show({
  title: 'Error',
  message: 'Something went wrong',
  color: 'red',
  icon: <XIcon size={16} />,
});

// Info notification
notifications.show({
  title: 'Information',
  message: 'Here is some info',
  color: 'blue',
  icon: <InfoIcon size={16} />,
});

// Warning notification
notifications.show({
  title: 'Warning',
  message: 'Please be careful',
  color: 'yellow',
  icon: <AlertTriangleIcon size={16} />,
});
```

---

## 🎠 **Carousel**

### **Purpose**

Enhanced carousel component for displaying images, content, or any elements in a
sliding interface.

### **Features**

- ✅ **Infinite Loop** - Seamless looping through items
- ✅ **Touch Support** - Swipe gestures on mobile
- ✅ **Keyboard Navigation** - Arrow key support
- ✅ **Indicators** - Visual progress indicators
- ✅ **Controls** - Previous/next buttons
- ✅ **Responsive** - Adapts to container size

### **Usage**

```typescript
import { Carousel } from '@mantine/core';

function MyCarousel() {
  return (
    <Carousel
      withIndicators
      loop
      withControls
      height={300}
    >
      <Carousel.Slide>
        <img src="/image1.jpg" alt="Slide 1" />
      </Carousel.Slide>
      <Carousel.Slide>
        <img src="/image2.jpg" alt="Slide 2" />
      </Carousel.Slide>
      <Carousel.Slide>
        <img src="/image3.jpg" alt="Slide 3" />
      </Carousel.Slide>
    </Carousel>
  );
}
```

### **Configuration Options**

```typescript
<Carousel
  radius="md"              // Border radius
  withIndicators={true}    // Show progress indicators
  loop={true}              // Enable infinite loop
  withControls={true}      // Show navigation controls
  controlSize="lg"         // Control button size
  height={300}             // Carousel height
  slideSize="100%"         // Slide width
  slideGap="md"            // Gap between slides
  align="start"            // Slide alignment
  slidesToScroll={1}       // Number of slides to scroll
/>
```

---

## ✍️ **RichTextEditor (TipTap-based)**

### **Purpose**

Advanced rich text editor built on TipTap with comprehensive formatting options
and modern UI.

### **Features**

- ✅ **Rich Formatting** - Bold, italic, underline, strikethrough
- ✅ **Headings** - H1-H6 heading levels
- ✅ **Lists** - Ordered and unordered lists
- ✅ **Links** - URL insertion and editing
- ✅ **Code Blocks** - Inline code and code blocks
- ✅ **Tables** - Insert and edit tables
- ✅ **Images** - Image insertion and management
- ✅ **Blockquotes** - Quote formatting
- ✅ **Placeholder Text** - Custom placeholder content

### **Usage**

```typescript
import { RichTextEditor } from '@mantine/core';

function MyEditor() {
  const [content, setContent] = useState('');

  return (
    <RichTextEditor
      value={content}
      onChange={setContent}
      placeholder="Start writing..."
    >
      <RichTextEditor.Toolbar>
        <RichTextEditor.ControlsGroup>
          <RichTextEditor.Bold />
          <RichTextEditor.Italic />
          <RichTextEditor.Underline />
          <RichTextEditor.Strikethrough />
        </RichTextEditor.ControlsGroup>

        <RichTextEditor.ControlsGroup>
          <RichTextEditor.H1 />
          <RichTextEditor.H2 />
          <RichTextEditor.H3 />
        </RichTextEditor.ControlsGroup>

        <RichTextEditor.ControlsGroup>
          <RichTextEditor.BulletList />
          <RichTextEditor.OrderedList />
        </RichTextEditor.ControlsGroup>

        <RichTextEditor.ControlsGroup>
          <RichTextEditor.Link />
          <RichTextEditor.Unlink />
        </RichTextEditor.ControlsGroup>

        <RichTextEditor.ControlsGroup>
          <RichTextEditor.Code />
          <RichTextEditor.CodeBlock />
        </RichTextEditor.ControlsGroup>
      </RichTextEditor.Toolbar>

      <RichTextEditor.Content />
    </RichTextEditor>
  );
}
```

### **Available Controls**

```typescript
// Text formatting
<RichTextEditor.Bold />
<RichTextEditor.Italic />
<RichTextEditor.Underline />
<RichTextEditor.Strikethrough />

// Headings
<RichTextEditor.H1 />
<RichTextEditor.H2 />
<RichTextEditor.H3 />
<RichTextEditor.H4 />
<RichTextEditor.H5 />
<RichTextEditor.H6 />

// Lists
<RichTextEditor.BulletList />
<RichTextEditor.OrderedList />

// Links
<RichTextEditor.Link />
<RichTextEditor.Unlink />

// Code
<RichTextEditor.Code />
<RichTextEditor.CodeBlock />

// Other
<RichTextEditor.Blockquote />
<RichTextEditor.Hr />
<RichTextEditor.ClearFormatting />
```

### **Configuration Options**

```typescript
<RichTextEditor
  radius="md"              // Border radius
  size="md"                // Editor size
  placeholder="Start writing..."  // Placeholder text
  value={content}          // Editor content
  onChange={setContent}    // Change handler
  readOnly={false}         // Read-only mode
  minHeight={200}          // Minimum height
  maxHeight={400}          // Maximum height
/>
```

---

## 🎨 **Styling Customization**

### **CSS Variables**

All extensions use Mantine CSS tokens sourced from Tailwind:

```css
/* Modal */
--mantine-radius-md: 0.375rem;
--mantine-shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
--mantine-spacing-md: 1rem;

/* Notifications */
--mantine-color-gray-2: #e5e7eb;
--mantine-color-blue-5: #3b82f6;
--mantine-color-green-5: #22c55e;

/* Carousel */
--mantine-transition-duration: 0.2s;
--mantine-transition-timing: ease;

/* RichTextEditor */
--mantine-font-family-monospace: ui-monospace, SFMono-Regular, monospace;
--mantine-color-gray-1: #f3f4f6;
```

### **Custom Themes**

```typescript
import { createTheme } from "@mantine/core";

const customTheme = createTheme({
  components: {
    ModalProvider: {
      styles: (theme) => ({
        content: {
          backgroundColor: theme.colors.gray[0],
          border: `1px solid ${theme.colors.gray[2]}`
        }
      })
    },
    NotificationsProvider: {
      styles: (theme) => ({
        notification: {
          backgroundColor: theme.colors.white,
          boxShadow: theme.shadows.md
        }
      })
    }
  }
});
```

---

## 🚀 **Best Practices**

### **Performance**

1. **Lazy Load Modals** - Only render modal content when needed
2. **Limit Notifications** - Don't show too many notifications at once
3. **Optimize Images** - Use optimized images in carousels
4. **Debounce Editor** - Debounce editor onChange events

### **Accessibility**

1. **Keyboard Navigation** - All extensions support keyboard navigation
2. **Screen Readers** - Proper ARIA labels and roles
3. **Focus Management** - Automatic focus management in modals
4. **Color Contrast** - High contrast ratios for readability

### **Mobile Experience**

1. **Touch Gestures** - Carousel supports swipe gestures
2. **Responsive Design** - All extensions are mobile-friendly
3. **Touch Targets** - Adequate touch target sizes
4. **Viewport Adaptation** - Modals adapt to mobile viewports

---

## 📦 **Installation & Setup**

### **Dependencies**

```bash
npm install @mantine/core @mantine/hooks @tiptap/react @tiptap/pm @tiptap/starter-kit
```

### **Provider Setup**

```typescript
import { MantineProvider } from '@mantine/core';
import { ModalProvider } from '@mantine/core';
import { NotificationsProvider } from '@mantine/core';
import { theme } from './theme';

function App() {
  return (
    <MantineProvider theme={theme}>
      <ModalProvider>
        <NotificationsProvider>
          <YourAppContent />
        </NotificationsProvider>
      </ModalProvider>
    </MantineProvider>
  );
}
```

---

## 🎯 **Conclusion**

The Mantine v8 extensions provide powerful, accessible, and beautifully styled
components that integrate seamlessly with the Tailwind design system. They
offer:

- **Global State Management** - ModalProvider and NotificationsProvider
- **Enhanced User Experience** - Smooth animations and interactions
- **Rich Content Creation** - TipTap-based RichTextEditor
- **Modern UI Patterns** - Carousel with touch support
- **Consistent Design** - Tailwind-like styling throughout

These extensions extend the core Mantine functionality while maintaining the
same design principles and accessibility standards.
