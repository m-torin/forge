/**
 * App-specific routing configuration
 * Re-exports the routing configuration from the internationalization package
 */

export { locales, routing, type Locale } from '@repo/internationalization/routing';
