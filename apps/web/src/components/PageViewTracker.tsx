"use client";

interface PageViewTrackerProps {
  locale?: string;
  userId?: string;
}

export function PageViewTracker({
  locale: _locale,
  userId: _userId,
}: PageViewTrackerProps) {
  return null;
}
