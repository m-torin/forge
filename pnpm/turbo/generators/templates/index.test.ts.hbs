import { describe, it, expect } from 'vitest';
import { hello } from '../src/index.ts';

describe('@repo/{{ name }}', () => {
it('should return the correct greeting', () => {
expect(hello()).toBe('Hello from @repo/{{ name }}');
});
});